// one stack for back button
// one stack for forward button

// every time a new page is opened(?)
  // old page gets pushed into back btn stack
// if back button is pressed,
  // current page gets pushed into forward btn stack
  // page pops from the back btn stack
// if forward button is pressed,
  // current page gets pushed into back btn stack
  // page pops from the foward btn stack